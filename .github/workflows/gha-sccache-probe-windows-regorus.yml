name: gha-sccache-probe-windows-regorus

on:
  workflow_dispatch:
  push:
    tags:
      - "gha-sccache-win-regorus-v*"

concurrency:
  group: gha-sccache-probe-windows-regorus-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  windows-regorus:
    name: windows-regorus
    runs-on: windows-2025
    timeout-minutes: 30
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_TERM_COLOR: always
      RUSTC_WRAPPER: sccache
      SCCACHE_CACHE_SIZE: 10G
      SCCACHE_DIR: ${{ github.workspace }}\.sccache
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: "1.95.0-x86_64-pc-windows-msvc"

      - name: Restore sccache directory
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}\.sccache
          key: gha-sccache-windows-regorus-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml', 'crates/scryer-rules/Cargo.toml') }}
          restore-keys: |
            gha-sccache-windows-regorus-

      - name: Install sccache
        uses: mozilla-actions/sccache-action@v0.0.10

      - name: Prime sccache server
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          sccache --start-server
          sccache --zero-stats

      - name: Build scryer-rules with sccache
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          cargo build -vv -p scryer-rules --locked 2>&1 | Tee-Object -FilePath scryer-rules-build.log
          if ($LASTEXITCODE -ne 0) {
            exit $LASTEXITCODE
          }

      - name: Show sccache stats
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          sccache --show-stats | Tee-Object -FilePath sccache-stats.txt

      - name: Upload probe artifacts
        uses: actions/upload-artifact@v7
        with:
          name: gha-sccache-probe-windows-regorus-${{ github.run_id }}
          path: |
            scryer-rules-build.log
            sccache-stats.txt
          if-no-files-found: error
          retention-days: 14
